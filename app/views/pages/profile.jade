div(id="page_content")
	h2 Attendee Profile
	div(class="attendee-avatar", style="background:url({{ pic }})")
	div(id="attendee-profile-map")
	div(class="if-mobile", id="profile-controls")
		a(class="note-button button", href="/notes/{{ user_id }}") Your Notes on {{ first_name }}
		a(class="follow-button button", data-user_id="{{ user_id }}")
	h3 {{ first_name }} {{ last_name }}
	div(class="attendee-from")
		| is making a <span class="ceil">{{ distance }}</span> mile expedition to WDS
		| all the way from {{ location }}
	div(class="connect") {{ connect }}
	div(class="line-canvas")
	div(id="profile-interested-in-shell")
		h3 {{ first_name }} is interested in: 
		div(class="attendee-interest-list") {{ interests }}
		div(class="line-canvas")
	div(id="profile-questions-shell")
		h3 A bit about {{ first_name }}:
		div(class="attendee-questions") {{ questions }}
		div(class="line-canvas")
	div(id="profile-dispatch-shell")
		h3 {{ first_name }}'s Dispatch
		div(class="dispatch", data-channel_type="user", data-channel_id='{{ user_id }}')



